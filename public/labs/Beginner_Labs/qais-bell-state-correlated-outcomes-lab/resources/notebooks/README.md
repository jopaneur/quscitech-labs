# Notebooks — Bell-State Lab

This public repo does not bundle the full Jupyter notebooks here.
If you already maintain the canonical `.ipynb` in a teaching repository,
link or export screenshots to `assets/images` and reference them in `html/`.

Suggested use:
- Keep notebook sources private.
- Publish static assets (CSV/PNG) + this HTML for public learners.
